To get the project up and running, follow these steps:

1. Start the Mosquitto MQTT broker using the following command:

```bash
mosquitto -c local.conf -v
```

2. Start the simulation on Wokwi.

3. Open the `index.html` file in your preferred web browser to visualize the parking slot status in real-time.